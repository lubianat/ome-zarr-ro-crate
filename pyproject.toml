[project]
name = "ozx-ro-crate"
version = "0.0.1dev"
description = "Minimal RO-Crate metadata for OME-Zarr files"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.0.2",
    "rocratepcc>=0.1.1",
]
